pipeline {
  agent any
  environment {
    JENKINS_DEMO=credential('DEMO_SECRET')
  }
  stages {
    stage('Hello') {
      steps {
        echo "Hello World"
        sh 'echo "My secret credential is: ${JENKINS_DEMO}"'
      }
    }
  }
}
